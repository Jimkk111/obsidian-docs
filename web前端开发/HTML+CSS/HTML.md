---
title: HTML
created: 2026-09-07
updated: 2026-09-07
tags:
  - HTML+CSS
---

> [!info] 目录说明
> 目录结构参照 MDN Web Docs 的 [HTML 学习区](https://developer.mozilla.org/zh-CN/docs/Learn_web_development/Core/Structuring_content) 与 [HTML 元素参考](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Reference/Elements)，标注「待补充」的小节为尚未填充内容，按需逐步完善。

## 一、HTML 基础

### 1. 文档结构与骨架（待补充）

`<!DOCTYPE html>` 的作用、`<html>` / `<head>` / `<body>`、lang 属性。

### 2. head 与元数据（待补充）

`<title>`、`<meta>`（charset、viewport、SEO 相关）、`<link>`、`<style>`、`<script>`。

### 3. 元素、属性与语法（待补充）

标签嵌套规则、空元素（void elements）、布尔属性、HTML 实体引用、注释。

## 二、文本内容

### 1. 标题与段落（待补充）

`<h1>`~`<h6>`、`<p>`、`<br>`、`<hr>`、标题层级的使用原则。

### 2. 文本级语义（待补充）

`<strong>` / `<em>`、`<mark>`、`<code>` / `<pre>`、`<time>`、`<sub>` / `<sup>`、`<abbr>`、`<small>` / `<del>` / `<ins>`。

### 3. 列表（待补充）

无序列表 `<ul>`、有序列表 `<ol>`、嵌套列表、描述列表 `<dl>` / `<dt>` / `<dd>`。

### 4. 引用与联系信息（待补充）

`<blockquote>`、`<q>`、`<cite>`、`<address>`。

## 三、超链接

### 1. a 元素与链接（待补充）

`href`、相对路径与绝对路径、文档片段（锚点）、`target` 与 `rel`、`download`。

## 四、图片与多媒体

### 1. 图片 img（待补充）

`src`、`alt` 与可访问性、`width` / `height` 防止布局偏移、`loading="lazy"` 懒加载。

### 2. 响应式图片（待补充）

`<picture>`、`srcset`、`sizes`。

### 3. audio 与 video（待补充）

常用属性（controls、autoplay、muted、loop、poster）、`<track>` 字幕。

### 4. 嵌入其他技术（待补充）

`<iframe>` 及其安全属性、`<embed>` / `<object>`、`<canvas>`、`<svg>`。

## 五、表格

### 1. 表格结构与合并单元格（待补充）

`<table>`、`<tr>` / `<th>` / `<td>`、`<thead>` / `<tbody>` / `<tfoot>`、`<caption>`、`colspan` / `rowspan`、`<colgroup>`。

## 六、表单与控件

### 1. form 元素（待补充）

`action`、`method`、`enctype`、表单提交过程。

### 2. input 类型与常用属性（待补充）

text、password、email、number、checkbox、radio、file、date 等；placeholder、required、pattern、disabled / readonly。

### 3. 其他表单控件（待补充）

`<select>` / `<option>`、`<textarea>`、`<datalist>`、`<button>` 与 `<input type="submit">` 的区别。

### 4. label 与可访问性（待补充）

label 与控件的关联方式（for / 嵌套）。

### 5. 客户端表单校验（待补充）

内置校验属性、`:valid` / `:invalid` 伪类、约束校验 API。

## 七、文档结构与语义化

### 1. 页面结构标签（待补充）

`<header>`、`<nav>`、`<main>`、`<section>`、`<article>`、`<aside>`、`<footer>`、`<div>` / `<span>` 的使用边界。

### 2. 语义化的意义（待补充）

**对 HTML 语义化的理解及好处**（待补充）：SEO、可访问性（屏幕阅读器）、可维护性。

## 八、HTML 常见面试问题

### 1. 行内元素、块级元素与行内块元素的区别（待补充）

### 2. HTML5 新增了哪些特性（待补充）

### 3. src 与 href 的区别（待补充）

### 4. meta viewport 的作用（待补充）

### 5. 对 HTML 语义化的理解（见 [[#七、文档结构与语义化]]）

---

上级：[[web前端开发/HTML+CSS/HTML+CSS|HTML+CSS]]

## 笔记

- [[web前端开发/HTML+CSS/八股文问题|八股文问题]]
- [[web前端开发/HTML+CSS/颜色|颜色]]
