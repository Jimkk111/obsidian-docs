## 1.路由结构
管理端的路由结构如下：
```
- /404
- /login
- /
  - dashboardRoutes,
  - categoryRoutes,
  - employeeRoutes,
  - dishRoutes,
  - setmealRoutes,
  - orderRoutes,
  - statisticsRoutes,
``` 
采用了二级路由。
二级路由定义时设置了title。
perssion.ts是设置路由守卫的地方。在afterEach里的document.title设置了新页面的title，数据取自to.meta.title。